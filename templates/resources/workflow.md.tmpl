{{/*
This is the default tfplugindocs resource template, with the single "Example
Usage" section replaced by one titled section per example file, in the order
they should be read.

Workflows are big enough that a single code block containing every example is
hard to navigate, so each example in examples/resources/incident_workflow gets
its own section here. If you add an example file, add a section for it below,
otherwise it won't appear in the docs.
*/ -}}
---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "{{.Name}} {{.Type}} - {{.RenderedProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

## Example - Assign the incident lead when an escalation is acked

{{ tffile "examples/resources/incident_workflow/resource-assign-incident-lead.tf" }}

## Example - Page execs from a manual workflow, using form fields

{{ tffile "examples/resources/incident_workflow/resource-page-execs-with-a-form.tf" }}

## Example - Create the postmortem follow-up when an incident closes

{{ tffile "examples/resources/incident_workflow/resource-postmortem-follow-up.tf" }}

## Example - Invite the security responders to an incident

{{ tffile "examples/resources/incident_workflow/resource-invite-security-responders.tf" }}

## Example - Share incident updates with leadership

{{ tffile "examples/resources/incident_workflow/resource-share-updates-with-leadership.tf" }}

## Example - Subscribe CSMs to customer-facing incidents

{{ tffile "examples/resources/incident_workflow/resource-subscribe-csms.tf" }}

{{ .SchemaMarkdown | trimspace }}
{{- if or .HasImport .HasImportIDConfig .HasImportIdentityConfig }}

## Import

Import is supported using an [`import` block](https://developer.hashicorp.com/terraform/language/import) or the [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import):
{{- end }}
{{- if .HasImportIdentityConfig }}

The [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

{{tffile .ImportIdentityConfigFile }}

{{ .IdentitySchemaMarkdown | trimspace }}
{{- end }}
{{- if .HasImportIDConfig }}

The [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

{{tffile .ImportIDConfigFile }}
{{- end }}
{{- if .HasImport }}

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

{{codefile "shell" .ImportFile }}
{{- end }}
