{{/*
This is the default tfplugindocs resource template, with the single "Example
Usage" section replaced by one titled section per example file, in the order
they should be read.

A policy's config differs by type, so each type gets its own example in
examples/resources/incident_policy and its own section here rather than being
run together in one block. If you add an example file, add a section for it
below, otherwise it won't appear in the docs.
*/ -}}
---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "{{.Name}} {{.Type}} - {{.RenderedProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

## Example - Require a post-mortem within five working days

{{ tffile "examples/resources/incident_policy/resource-post-mortems.tf" }}

## Example - Check that on-call responders can be reached

{{ tffile "examples/resources/incident_policy/resource-on-call-readiness.tf" }}

## Example - Flag responders rota'd on while they are away

{{ tffile "examples/resources/incident_policy/resource-vacation-conflicts.tf" }}

{{ .SchemaMarkdown | trimspace }}
{{- if or .HasImport .HasImportIDConfig .HasImportIdentityConfig }}

## Import

Import is supported using an [`import` block](https://developer.hashicorp.com/terraform/language/import) or the [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import):
{{- end }}
{{- if .HasImportIdentityConfig }}

The [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

{{tffile .ImportIdentityConfigFile }}

{{ .IdentitySchemaMarkdown | trimspace }}
{{- end }}
{{- if .HasImportIDConfig }}

The [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

{{tffile .ImportIDConfigFile }}
{{- end }}
{{- if .HasImport }}

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

{{codefile "shell" .ImportFile }}
{{- end }}
